identity:
  name: add_zep_memory
  author: obadakhalili
  label:
    en_US: Add memory to session

description:
  human:
    en_US: Add memory to user in a specified session
  llm: Add memory to user in a specified session

parameters:
  - name: session_id
    type: string
    required: true
    label:
      en_US: Session ID
    human_description:
      en_US: The session ID the memory will be stored in
    llm_description: The session ID the memory will be stored in
    form: llm
  - name: user_message
    type: string
    required: true
    label:
      en_US: User Message
    human_description:
      en_US: User message from conversation
    llm_description: User message from conversation
    form: llm
  - name: assistant_response
    type: string
    required: true
    label:
      en_US: Assistant Response
    human_description:
      en_US: Assistant response to user message
    llm_description: Assistant response to user message
    form: llm

extra:
  python:
    source: tools/add_session_memory.py
